# Rule: Frontend Quality

- Preserve existing design systems when they already exist.
- For new work, avoid boilerplate layouts and default type stacks.
- Use clear color systems, intentional hierarchy, and responsive behavior.
- Verify accessibility basics: semantics, focus handling, contrast, and alt text where needed.
- Keep motion meaningful and sparse.
